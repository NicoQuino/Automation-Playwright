{
  "name": "automation-playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:chromium": "playwright test --config=playwright.config.ts --project=chromium",
    "test:chromium:h": "playwright test --config=playwright.config.ts --project=chromium --headed"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.55.0"
  }
}
